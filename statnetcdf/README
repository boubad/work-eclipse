Project statnetcdf static lib

Description:
        netcdf storage of statistical datasets

Dependencies:
            project statgen
            project statdata

External dependencies:
            boost c++ (headers), netcdf library

Global compiler switches:
            -std=c++11 or -std=c++0x
            -fopenmp

Global include directories:
            ../statgen/include
            ../statdata/include

